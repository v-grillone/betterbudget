{
  "name": "betterbudget-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile"
  }
}
